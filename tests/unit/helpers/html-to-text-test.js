import { htmlToText } from '../../../helpers/html-to-text';
import { module, test } from 'qunit';

module('Unit | Helper | html to text');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = htmlToText(42);
  assert.ok(result);
});
