import Ember from 'ember';

export default Ember.Component.extend({
    tagName: 'pre',
    classNames: ['script-renderer'],
    source: [
        '// generated by http://blackmesasource.com/qc v0.0.0',
        '',
        '$modelname         "props_sdk\\myfirstmodel.mdl"',
        '$body xmybody      "myfirstmodel-ref.smd"',
        '$staticprop',
        '$surfaceprop       combine_metal',
        '$cdmaterials       "models\props_sdk"',
        '',
        '// no animation wanted, so re-using the reference mesh',
        '$sequence idle     "myfirstmodel-ref.smd"',
        '',
        '$collisionmodel    "myfirstmodel-phys.smd" {',
        '   $concave',
        '}'
    ].join('\n'),

    didInsertElement: function () {
        /* global hljs */
        hljs.highlightBlock(this.get('element'));
    }
});
